package com.mitsugaru.Cliques.commands;

public enum ValidCommands
{
	//Base sub commands
	CREATE, JOIN, LEAVE, KICK, INVITE, POST, WALL, VERSION, VER, HELP,
	//Edit sub commands
	EDIT, PREFIX, PVP,
	//Admin sub commands
	ADMIN, DELETE, RELOAD, ADD;
}
